import './WeatherCard.css'
const WeatherCard = ({ weather, temps }) => {
  // console.log(weather)

  return (
    <div className="weathercardsection">
      {/* <h1>{weather.name}</h1>
      <h1>{temps.temp}</h1>
      <h1>{temps.feels_like}</h1> */}
    </div>
  )
}

export default WeatherCard
